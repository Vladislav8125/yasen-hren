import Link from "next/link";
import { redirect } from "next/navigation";
import { auth } from "@/auth";
import { NIIH_TEAM } from "@/data/knowledge";
export default async function KnowledgeTeamPage() { const session = await auth(); if (!session?.user) redirect("/login"); return <div className="mx-auto w-full max-w-5xl p-6 py-10 md:p-12"><Link href="/knowledge" className="font-technical text-xs uppercase tracking-widest text-gold">← НИИХ база знаний</Link><p className="mt-10 font-technical text-xs uppercase tracking-widest text-gold">Люди института</p><h1 className="mt-2 font-display text-4xl text-parchment-hi">Команда НИИХ</h1><p className="mt-4 max-w-2xl font-body text-lg text-bone-dim">Научно-исследовательский институт хренологии собирает методологию, игру и практику в одну живую систему.</p><div className="mt-10 grid gap-5 md:grid-cols-2">{NIIH_TEAM.map((member) => <article key={member.name} className="rounded-2xl border border-void-border bg-void-elevated p-6"><div className="mb-5 flex h-16 w-16 items-center justify-center rounded-full bg-gold/15 font-display text-2xl text-gold">{member.name[0]}</div><h2 className="font-display text-2xl text-parchment-hi">{member.name}</h2><p className="mt-1 font-technical text-xs uppercase tracking-widest text-gold">{member.role}</p><p className="mt-4 font-body leading-relaxed text-bone">{member.bio}</p></article>)}</div></div>; }
